<% @cue_sheet.cues.each do |cue| -%>
<% if cue.roads && !cue.start? -%>
<%= cue.block_distance_to_here.to_f %>km先(<%= cue.prev.roads.map { |r| "#{r}〜" }.join %>) :<%= cue.percent %>
<% end -%>
<%= cue.no %>: <%= cue.point.as_is || ("🚥#{"[#{cue.point.signal_name}]" if cue.point.signal_name }") %> <%= cue.direction %> <%= cue.road_to.try! { |next_road| "#{next_road}に" } %>
<% if cue.other -%>
(<%= cue.other %>)
<% end -%>

<% end -%>
